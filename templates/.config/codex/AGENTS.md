Please provide all answers in Japanese

{{ read_file(path="extras/ai-agent/ai_system_prompt.md") }}

## File Type Specific Rules

Apply the following rules only when editing files that match each pattern.

### Dockerfile (`**/Dockerfile`)

{{ read_file(path="extras/ai-agent/rules/dockerfile.md") }}

### GitHub Actions (`.github/{actions,workflows}/*.{yaml,yml}`)

{{ read_file(path="extras/ai-agent/rules/github-actions.md") }}
